# cto-brain — growth ledger (append-only)

Format: `YYYY-MM-DD | <round-tag> | <action-summary> | <lesson-or-noop>`

Project-specific lessons stay here. General brain policy → system `~/.cto-brain/memory/growth_ledger.md` via `cto-brain sync --promote`.

2026-06-24 | ship-0.1.2 | bumped 0.1.1→0.1.2; npm 0.1.1 was stale (published from 8ee880e, before adapter feature b3901ea) | npm published versions are immutable — a stale publish forces a version bump, never a republish; check `npm view <pkg> gitHead` vs HEAD before claiming "shipped"
2026-06-24 | ship-0.1.2 | gh secret list empty → release.yml NPM_TOKEN missing | tag-driven release silently fails at publish without the secret; verify secrets before pushing the tag
2026-06-24 | mcp-roundA | added MCP server (src/mcp/tools+server, `cto-brain mcp`); wrapped 7 existing fns as MCP tools; first runtime dep @modelcontextprotocol/sdk | gate check already excludes node_modules so first dep didn't break the security gate; reviewer trio caught ledger log-injection in round_close (sanitize tool args) + missing e2e transport test (added) — unit-testing a registry without the transport is a real coverage gap
2026-06-24 | telemetry-roundB | added local JSONL run telemetry (src/telemetry/recorder) wired through cli/router + mcp/tools; `cto-brain telemetry summary`; bump 0.2.0 | telemetry at the CLI/MCP boundary keeps pure fns pure; reviewer trio caught test-pollution (a spawned-subprocess test must set CTO_BRAIN_HOME or it writes telemetry to the real home during CI) + that latencyMs conflates probe round-trips with routing — document KPI semantics rather than overclaim
2026-06-25 | ship-0.2.0 | published 0.2.0 to npm via manual `npm publish` (prepublishOnly ran gate + 15 suites green); npm latest=0.2.0, published gitHead==HEAD (46381f8), origin/main in sync — no stale-publish trap this time | manual `npm publish` and tag-driven release.yml are BOTH armed; pushing a v* tag after a manual publish would fire a doomed re-publish run (npm versions immutable) — pick ONE release path per version, or gate the workflow on "version not already on npm"
2026-06-25 | portfolio-wire | wired last 2 bare projects (Wedding, aminichain) → all 7 live projects now have .cto-brain + 4 skills on claude-code+cursor | reaching full portfolio coverage is cheap once the adapter wire flow is stable; remaining gap is operator-machine global CLI still 0.1.1 (needs npm i -g cto-brain@latest)
