# cto-brain — growth ledger (append-only)

Format: `YYYY-MM-DD | <round-tag> | <action-summary> | <lesson-or-noop>`

Project-specific lessons stay here. General brain policy → system `~/.cto-brain/memory/growth_ledger.md` via `cto-brain sync --promote`.

2026-06-24 | ship-0.1.2 | bumped 0.1.1→0.1.2; npm 0.1.1 was stale (published from 8ee880e, before adapter feature b3901ea) | npm published versions are immutable — a stale publish forces a version bump, never a republish; check `npm view <pkg> gitHead` vs HEAD before claiming "shipped"
2026-06-24 | ship-0.1.2 | gh secret list empty → release.yml NPM_TOKEN missing | tag-driven release silently fails at publish without the secret; verify secrets before pushing the tag
2026-06-24 | mcp-roundA | added MCP server (src/mcp/tools+server, `cto-brain mcp`); wrapped 7 existing fns as MCP tools; first runtime dep @modelcontextprotocol/sdk | gate check already excludes node_modules so first dep didn't break the security gate; reviewer trio caught ledger log-injection in round_close (sanitize tool args) + missing e2e transport test (added) — unit-testing a registry without the transport is a real coverage gap
2026-06-24 | telemetry-roundB | added local JSONL run telemetry (src/telemetry/recorder) wired through cli/router + mcp/tools; `cto-brain telemetry summary`; bump 0.2.0 | telemetry at the CLI/MCP boundary keeps pure fns pure; reviewer trio caught test-pollution (a spawned-subprocess test must set CTO_BRAIN_HOME or it writes telemetry to the real home during CI) + that latencyMs conflates probe round-trips with routing — document KPI semantics rather than overclaim
2026-06-25 | ship-0.2.0 | published 0.2.0 to npm via manual `npm publish` (prepublishOnly ran gate + 15 suites green); npm latest=0.2.0, published gitHead==HEAD (46381f8), origin/main in sync — no stale-publish trap this time | manual `npm publish` and tag-driven release.yml are BOTH armed; pushing a v* tag after a manual publish would fire a doomed re-publish run (npm versions immutable) — pick ONE release path per version, or gate the workflow on "version not already on npm"
2026-06-25 | portfolio-wire | wired last 2 bare projects (Wedding, aminichain) → all 7 live projects now have .cto-brain + 4 skills on claude-code+cursor | reaching full portfolio coverage is cheap once the adapter wire flow is stable; remaining gap is operator-machine global CLI still 0.1.1 (needs npm i -g cto-brain@latest)
2026-06-25 | agent-prompt-verify | engineered a no-repo-access steward system prompt (docs/AGENT-SYSTEM-PROMPT.md) + GROWTH-AGENT-BRIEF + POSITIONING; verified the "how it's built" facts against v0.2.0 using cto-brain's OWN tools (router plan/list, adapter list, live MCP listTools, CLI usage) | verify any architecture/"how it's built" doc against the product's own tool output, not memory — tool-verification is the gate that catches drift before it ships
2026-06-25 | research-correct | deep-research adversarial pass (27 sources, 25 3-vote-verified claims) corrected a conflation I'd shipped in AGENT-SYSTEM-PROMPT §5 (Conductor ~7B/RL ≠ TRINITY ~0.6B/CMA-ES) + dropped unverified Fugu benchmark numbers; saved docs/research/agentic-orchestration-2026.md | quick web-search grounding is NOT 3-vote verification — for load-bearing EXTERNAL claims run the adversarial verify BEFORE baking facts into shipped docs; separate the two distinct artifacts behind a product before describing it
2026-06-25 | eval-0.3.0 | added eval harness (src/eval, `cto-brain eval`, eval_run MCP tool, gated in CI) scoring routing/honesty/gate — 11 cases, passRate 1.0; probe now respects enabledProviders so the desk-engine 403 is quieted; npm-docs (README) polish | an eval suite's FIRST value is exposing your own wrong expectations, not the code's bugs — it caught a bad fallbackUsed assumption (spec was wrong, brain was honest); reviewer trio (security + tech-lead) both CLEAN, 0 must-fix
2026-06-25 | cli-ux-0.4.0 | human-readable colorized CLI output (src/cli/ui+render) for router/stack/adapter/telemetry/eval; grouped --help; --json/--no-color; zero new deps | TTY→human, piped/--json→JSON keeps CI + tests byte-stable while interactive use is readable; ANSI-strip widths before alignment math; CLICOLOR_FORCE forces color but NOT format (pipe still = JSON) — format switch keys off isTTY/--json only
2026-06-25 | a2a-0.5.0 | Brick 1 delivered: A2A agent card (src/a2a/card, `cto-brain agent-card`, agent_card MCP tool → 9 tools, docs/A2A) derived from the live tool registry; ADR 0001 (transport+discovery) + ADR 0002 (skill-synthesis, design-first privacy boundary: gate-before-read, pattern-only, draft+human-review) | derive discovery metadata from the live registry so it can't drift; for a HIGH-risk brick (skill-synthesis reading a workspace) write the privacy ADR BEFORE any code — gate-before-read beats redact-after
2026-06-25 | benchmark-refresh | Brick 3 delivered: refreshed docs/benchmark to v0.5.0 (19 cmds, 8 tasks, 9 MCP tools, 18 suites/270 asserts, 1 dep, 369KB); added §B2 eval-based proof-of-performance; sharpened Fugu/Fable framing to the VERIFIED research (no fabricated head-to-head); fixed benchmark-self CLI counter regression (USAGE const → usage() broke the regex) | refactoring help from a const string to a function silently broke the self-benchmark's command scrape — when you change a surface another tool parses, grep for who parses it; the honest "prove vs others" answer for a policy layer is an internal falsifiable eval, not a borrowed model leaderboard
2026-06-25 | http-0.6.0 | Brick 2 delivered: MCP Streamable-HTTP transport (src/mcp/streamableHttp, `cto-brain mcp --transport=http`) — loopback-only default + Origin-403 DNS-rebinding guard; 9 tools over HTTP verified live; resumed cleanly from the bg-agent partial (valid+safe, finished inline); fixed --key=value arg parsing | OPERATE caught what CI missed — the test drove createStreamableHttpServer directly, so the CLI-flag bug (--transport=http unparsed → silently ran stdio) only surfaced when I launched the REAL server; always operate the CLI path, not just the unit. bg agents killed by session-limit leave valid partials worth resuming, not discarding
2026-06-25 | agent-docs | persisted the close-out report as docs/reports/2026-06-25-v0.9.0-closeout.md + a STATUS entry; added docs/AGENT-ONBOARDING.md (a zero-knowledge agent learns cto-brain from npm + github, honest about the version gap) | the close-out report must land as a durable MD — chat-only isn't a handoff; a fresh-agent onboarding doc should point the agent at LIVE tool output (run it, read the JSON) over trusting doc counts, and say plainly that npm (0.2.0) lags the repo
2026-06-25 | harden-0.9.0 | fixed the 3 prod gaps + better help: telemetry rotation (size cap → runs.jsonl.1, summary spans both), model-availability surfaced (route.modelAvailable + WARNING in reason, non-breaking), HTTP-concurrency mitigated (atomic line append + skip-bad-line); grouped --help gained an Examples block + help/-h; eval +2 cases (13/13) | a wrong TEST assertion can masquerade as a code bug — the rotation "failure" was my expectation (single-backup drops old history BY DESIGN), not the code; assert the real guarantee (spans backup+live, bounded), not an idealized one. for model-availability, surface-don't-refuse keeps it honest without blocking a valid-but-unlisted model
2026-06-25 | examples-0.8.0 | test-first conclusive usage suite (test/e2e-usage, 22 checks, in CI) that doubles as executable docs; docs/EXAMPLES.md shows the tool in a REAL round; made the operator close-out report a STANDING ritual — step 5 of round-close in cto-orchestration SKILL + docs/CLOSEOUT-REPORT.md + brain memory feedback-closeout-report-standing | document-by-testing — EXAMPLES.md mirrors e2e-usage so docs can't silently drift; the operator handoff report is now mandatory every working round (skill + memory + doc), per George
2026-06-25 | prod-readiness | reviewer trio (security GO / tech-lead GO-pending-docs / devil's-advocate GO-with-caveats) + clean-room install smoke (PASS, eval 11/11) on v0.7.0; fixed doc version drift across 6 files, added CHANGELOG + docs/PRODUCTION.md (3 known limits + deploy guidance), refreshed the roadmap, gitignored wired adapter dirs | the prod blocker was DOC HONESTY, not code — stale version pins + shipped-features-listed-as-roadmap drift; for "ready for prod?" the conclusive test is a clean-room tarball install (CI-green ≠ installable), and the honest deliverable is a GO with known-limitations documented, not a claim of perfection
2026-06-25 | synth-0.7.0 | Brick 4 delivered (bg agent built, parent reviewed+integrated): privacy-first skill synthesis (src/synth/discover+synthesize, `cto-brain skill synth`) per ADR 0002 — gate-before-read (dir+file level), provenance-only draft, refuse-on-secret/abs-path/URL/env, draft-only to .cto-brain/skills-draft (gitignored), human-review-before-promote; deliberately NOT an MCP tool (kept human-driven) | for a privacy feature the verification that counts is OPERATING the real command against a planted secret — ran skill synth over a workspace with sk- keys, it skipped them (1 candidate not 3) and emitted a secret-free draft; I manually code-reviewed both modules before trusting the high-risk brick, not just the agent's self-report
2026-06-25 | pulse-v0.1.0 | Built+shipped pulse (OSS uptime monitor: Rust/axum+SQLite+poller, SvelteKit SPA, npm client) design→v0.1.0 across 4 reviewer-gated rounds, orchestrated by cto-brain | operate + reviewer-trio caught what green tests missed: a 404 SPA-fallback bug, a per-connection FK-cascade no-op, and a no-WAL concurrency cliff — bring the stack up and run the trio before tagging
2026-06-25 | brain-learn-mattpocock | Studied mattpocock/skills; integrated 3 disciplines into the brain — grill-before-decompose (cto-orchestration Role 1), user/model skill invocation classes + composition rule (meta-brain), CONTEXT.md shared-vocabulary convention (+ templates/CONTEXT.md); synced to system + global brain | peer skill-sets are a free policy-improvement source — the grill lesson was already proven by the pulse build's 6 mid-flight scope adds; capture cross-repo lessons as brain skills, not just chat
2026-06-25 | skills-wired-agentic | Ported grill/tdd/diagnosing-bugs/domain-modeling as 4 native bundled skills (credited to mattpocock/skills) and wired them into the agentic workflow: cto-orchestration tool-table + Role 1 grill step, meta-brain catalog (IC disciplines, model-invoked) + composition rule, and agentic-learning-loop reward-signal coupling. Bundled 4->8; synced to system + global brain | a discipline isn't 'in the brain' until it's both a bundled skill AND wired into the workflow that fires it — catalog-only means it never gets invoked; wire at the decision surface (tool table) + the learning loop, not just the index
2026-06-26 | jarvis-code-terminal-p0 | Shipped cto-brain code: local-model coding terminal (capability picker + codex --oss). Fixed phantom stdin-TTY bug. jarvis backend == on-box ollama (bridge deferred to P1). | Verify interactive-TUI launches under a real PTY, never --help/non-TTY; empirically verify the integration wire (codex 0.142 dropped wire_api=chat); compare model lists before assuming 'remote'.
