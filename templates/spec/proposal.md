# Change: {{CHANGE_ID}}

**Status:** Proposed ({{DATE}}). Freeze before implementation; amend the spec
first when implementation proves it wrong — drift is a bug.

## Intent

## Behavior

## Acceptance criteria

## Non-goals

## Frozen contracts

(Schemas, interfaces, file layouts, API shapes agents code against. Lock
before fan-out; agents READ contracts, they don't EDIT them.)
